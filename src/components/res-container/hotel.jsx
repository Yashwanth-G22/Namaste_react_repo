import styled from "styled-components";

const StyledHotelWrapper = styled(div)`
    display: grid;
    grid-template-columns: repeat(2, auto);

`;

export const Hotel = () => {

    return (
        <StyledHotelWrapper>
           
        </StyledHotelWrapper>
    )
};
